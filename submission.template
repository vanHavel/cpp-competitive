#include <iostream>
#include <fstream>

%Code%

int main() {
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(NULL);
	std::cout << std::fixed;
	std::cout << std::setprecision(15);

	%ClassName% solver;
	%Input%
	%Output%
	%SolverCall%
	return 0;
}
